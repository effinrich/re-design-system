Read `GSTACK_LITE.md` before non-trivial work.
For React UI work, also read `gstack-lite-storybook.md`.

Use the slash-command modes from `.claude/skills/`:
- `/plan-ceo-review`
- `/plan-eng-review`
- `/review`
- `/ship`
- `/retro`

This repo is a React design-system / component-heavy UI repo.
Storybook is required.
Chromatic is required for shared UI changes in larger component-heavy surfaces.
